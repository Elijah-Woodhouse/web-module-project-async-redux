const initialState = {
    catFactData: [],
    loading: false,
    error: ""
}

const catFactReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return(state);
    }
}

export default catFactReducer;
